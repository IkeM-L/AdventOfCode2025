# How to use

Put your input in the same directory as the code, names Day{i}.txt or Day{i}Example.txt, then adjust constants at top of Program.cs

Input validation intentionally excluded due to the nature of the code's "run once" intent

Files are named to display in order when alphabetical

Example inputs are provided, actual inputs are not